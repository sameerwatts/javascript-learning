const max = Math.max(12, 45); // function call statement
const floor = Math.floor(1.77777);

// other build in methods
// parseFloat('2.4642'),
// parseInt('3.425465'),
// Date.now(), epoch
// Navigator.vibrate(200);
// build in for dom
// window.scrollTo(x-cords, y-cords);
const p = document.querySelector('p');
console.log(p);
